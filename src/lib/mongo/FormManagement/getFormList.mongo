function getFormData(args) {

    //  db.form.find();
    var data={}
    data.data=db.form.find().skip(parseInt(args.index)-1).limit(parseInt(args.limit)).sort({createTime:1}).toArray()
    data.totalCount=db.form.count()
    return  data

}