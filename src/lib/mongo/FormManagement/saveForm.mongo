function saveForm(args) {
    var mydate = new Date();
    var dates = ( mydate.getFullYear() + "/" + (mydate.getMonth() + 1) + "/" + mydate.getDate() + " " +
    mydate.getHours() + ":" + mydate.getMinutes() + ":" + mydate.getSeconds())
    if (!args._id) {
        var writeResult = {}
        var a = new ObjectId()
        args.createTime = dates;
        args.lastmodifyTime = dates;
        writeResult = db.form.insert(args)
        return {
            'WriteResult': JSON.parse(writeResult.tojson()),
            '_id': a
        }
    }
    else {
        var writeResult = {}
        args.lastmodifyTime = dates;
        writeResult = db.form.update(args)
        return {
            'WriteResult': JSON.parse(writeResult.tojson()),
            '_id': args._id
        }
    }
}



